name: firecrawl
networks:
  backend:
    driver: bridge
    name: firecrawl_backend
  reverse-proxy:
    external: true
    name: reverse-proxy
services:
  api:
    command: |
      sh -c " sed -i 's/getModelLimits(\"gpt-4o-mini\")/getModelLimits(\"gemini-2.5-flash-lite\")/g' /app/dist/src/scraper/scrapeURL/transformers/llmExtract.js && echo '✅ Patched: getModelLimits now uses gemini-2.5-flash-lite (1M input, 65K output)' && node dist/src/harness.js --start-docker "
    container_name: firecrawl
    cpus: 4
    depends_on:
      playwright-service:
        condition: service_started
        required: true
      rabbitmq:
        condition: service_healthy
        required: true
      redis:
        condition: service_started
        required: true
    environment:
      AUTUMN_SECRET_KEY: ${AUTUMN_SECRET_KEY}
      BROWSER_POOL_SIZE: ${BROWSER_POOL_SIZE:-5}
      BULL_AUTH_KEY: ${BULL_AUTH_KEY}
      CRAWL_CONCURRENT_REQUESTS: ${CRAWL_CONCURRENT_REQUESTS:-10}
      ENV: local
      EXTRACT_WORKER_PORT: ${EXTRACT_WORKER_PORT:-3004}
      FDB_CLUSTER_FILE: ${NUQ_BACKEND:+/var/fdb/fdb.cluster}
      HARNESS_STARTUP_TIMEOUT_MS: ${HARNESS_STARTUP_TIMEOUT_MS:-60000}
      HOST: 0.0.0.0
      LOGGING_LEVEL: ${LOGGING_LEVEL}
      MAX_CONCURRENT_JOBS: ${MAX_CONCURRENT_JOBS:-5}
      MODEL_EMBEDDING_NAME: ${MODEL_EMBEDDING_NAME}
      MODEL_NAME: ${MODEL_NAME}
      NUM_WORKERS_PER_QUEUE: ${NUM_WORKERS_PER_QUEUE:-8}
      NUQ_BACKEND: ${NUQ_BACKEND}
      NUQ_RABBITMQ_URL: amqp://rabbitmq:5672
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL}
      PLAYWRIGHT_MICROSERVICE_URL: ${PLAYWRIGHT_MICROSERVICE_URL:-http://playwright-service:3000/scrape}
      PORT: ${INTERNAL_PORT:-3002}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      POSTGRES_HOST: ${POSTGRES_HOST:-nuq-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      PROXY_PASSWORD: ${PROXY_PASSWORD}
      PROXY_SERVER: ${PROXY_SERVER}
      PROXY_USERNAME: ${PROXY_USERNAME}
      REDIS_RATE_LIMIT_URL: ${REDIS_RATE_LIMIT_URL:-${REDIS_URL:-redis://redis:6379}}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      SEARXNG_CATEGORIES: ${SEARXNG_CATEGORIES}
      SEARXNG_ENDPOINT: ${SEARXNG_ENDPOINT}
      SEARXNG_ENGINES: ${SEARXNG_ENGINES}
      SELF_HOSTED_WEBHOOK_URL: ${SELF_HOSTED_WEBHOOK_URL}
      SLACK_WEBHOOK_URL: ${SLACK_WEBHOOK_URL}
      SUPABASE_ANON_TOKEN: ${SUPABASE_ANON_TOKEN}
      SUPABASE_SERVICE_TOKEN: ${SUPABASE_SERVICE_TOKEN}
      SUPABASE_URL: ${SUPABASE_URL}
      TEST_API_KEY: ${TEST_API_KEY}
      USE_DB_AUTHENTICATION: ${USE_DB_AUTHENTICATION:-false}
      WORKER_PORT: ${WORKER_PORT:-3005}
    extra_hosts:
      - host.docker.internal:host-gateway
    image: jcr.savorcare.com/ghcr/firecrawl/firecrawl@sha256:1910ef022706238c7d13c0e191b9a080efcf10aab049d776ba321abd8dc512ca
    labels:
      - traefik.enable=true
      - traefik.http.routers.firecrawl.rule=Host(`firecrawl.savorcare.com`)
      - traefik.http.routers.firecrawl.entrypoints=websecure
      - traefik.http.routers.firecrawl.tls.certresolver=myresolver
      - traefik.http.routers.firecrawl.tls.domains[0].main=savorcare.com
      - traefik.http.routers.firecrawl.tls.domains[0].sans=*.savorcare.com
      - traefik.http.routers.firecrawlhttp.rule=Host(`firecrawl.savorcare.com`)
      - traefik.http.routers.firecrawlhttp.entrypoints=web
      - traefik.docker.network=reverse-proxy
    logging:
      driver: json-file
      options:
        compress: "true"
        max-file: "3"
        max-size: 10m
    mem_limit: 8G
    memswap_limit: 8G
    networks:
      backend: null
      reverse-proxy: null
    ports:
      - ${PORT:-3002}:${INTERNAL_PORT:-3002}
    restart: unless-stopped
    ulimits:
      nofile:
        hard: 65535
        soft: 65535
    volumes:
      - read_only: true
        source: fdb-cluster-file
        target: /var/fdb
        type: volume
        volume: {}
  foundationdb:
    environment:
      FDB_COORDINATOR_PORT: 4500
      FDB_NETWORKING_MODE: container
    image: jcr.savorcare.com/docker/foundationdb/foundationdb:7.3.63
    logging:
      driver: json-file
      options:
        compress: "true"
        max-file: "3"
        max-size: 10m
    networks:
      backend: null
    volumes:
      - source: fdb-data
        target: /var/fdb/data
        type: volume
        volume: {}
      - source: fdb-cluster-file
        target: /var/fdb
        type: volume
        volume: {}
  foundationdb-init:
    depends_on:
      foundationdb:
        condition: service_started
        required: true
    entrypoint:
      - /bin/bash
      - -c
      - |-
        sleep 5 && out=$(fdbcli -C /var/fdb/fdb.cluster --exec 'configure new single ssd' 2>&1); status=$$?; printf '%s
        ' "$$out"; if [ "$$status" -eq 0 ]; then exit 0; fi; printf '%s
        ' "$$out" | grep -Eiq 'already.*configured|database.*configured|database.*already exists'
    image: jcr.savorcare.com/docker/foundationdb/foundationdb:7.3.63
    networks:
      backend: null
    restart: "no"
    volumes:
      - source: fdb-cluster-file
        target: /var/fdb
        type: volume
        volume: {}
  nuq-postgres:
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
    image: jcr.savorcare.com/ghcr/firecrawl/nuq-postgres@sha256:aed86f62858f29bd971abddcdeb301c12888098d2cf5d33c1ba42b053bc460f6
    logging:
      driver: json-file
      options:
        compress: "true"
        max-file: "3"
        max-size: 10m
    networks:
      backend: null
    restart: unless-stopped
    volumes:
      - source: postgres
        target: /var/lib/postgresql/data
        type: volume
        volume: {}
  playwright-service:
    cap_drop:
      - ALL
    cpus: 2
    environment:
      ALLOW_LOCAL_WEBHOOKS: ${ALLOW_LOCAL_WEBHOOKS}
      BLOCK_MEDIA: ${BLOCK_MEDIA}
      MAX_CONCURRENT_PAGES: ${CRAWL_CONCURRENT_REQUESTS:-10}
      PORT: 3000
      PROXY_PASSWORD: ${PROXY_PASSWORD}
      PROXY_SERVER: ${PROXY_SERVER}
      PROXY_USERNAME: ${PROXY_USERNAME}
    image: jcr.savorcare.com/ghcr/firecrawl/playwright-service@sha256:df1a393ce8bfc3801570a826a9b0dcc400ae48789adb4a0ad9d6cbf0229b059b
    logging:
      driver: json-file
      options:
        compress: "true"
        max-file: "3"
        max-size: 10m
    mem_limit: 4G
    memswap_limit: 4G
    networks:
      backend: null
    ports:
      - mode: ingress
        protocol: tcp
        published: "3031"
        target: 3000
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp/.cache:noexec,nosuid,size=1g
  rabbitmq:
    command: rabbitmq-server
    healthcheck:
      interval: 5s
      retries: 3
      start_period: 5s
      test:
        - CMD
        - rabbitmq-diagnostics
        - -q
        - check_running
      timeout: 5s
    image: jcr.savorcare.com/docker/rabbitmq:3-management
    logging:
      driver: json-file
      options:
        compress: "true"
        max-file: "2"
        max-size: 5m
    networks:
      backend: null
    restart: unless-stopped
    volumes:
      - bind:
          create_host_path: true
        source: /tmp/firecrawl/rabiitmq
        target: /var/lib/rabbitmq
        type: bind
  redis:
    command: redis-server --bind 0.0.0.0
    healthcheck:
      interval: 5s
      retries: 10
      test:
        - CMD
        - redis-cli
        - ping
      timeout: 5s
    image: jcr.savorcare.com/docker/redis:alpine
    logging:
      driver: json-file
      options:
        compress: "true"
        max-file: "2"
        max-size: 5m
    networks:
      backend: null
    restart: unless-stopped
    volumes:
      - source: redis
        target: /data
        type: volume
        volume: {}
volumes:
  fdb-cluster-file:
    name: firecrawl_fdb-cluster-file
  fdb-data:
    name: firecrawl_fdb-data
  postgres:
    name: firecrawl_postgres
  redis:
    name: firecrawl_redis
x-common-env:
  AUTUMN_SECRET_KEY: ${AUTUMN_SECRET_KEY}
  BROWSER_POOL_SIZE: ${BROWSER_POOL_SIZE:-5}
  BULL_AUTH_KEY: ${BULL_AUTH_KEY}
  CRAWL_CONCURRENT_REQUESTS: ${CRAWL_CONCURRENT_REQUESTS:-10}
  FDB_CLUSTER_FILE: ${NUQ_BACKEND:+/var/fdb/fdb.cluster}
  LOGGING_LEVEL: ${LOGGING_LEVEL}
  MAX_CONCURRENT_JOBS: ${MAX_CONCURRENT_JOBS:-5}
  MODEL_EMBEDDING_NAME: ${MODEL_EMBEDDING_NAME}
  MODEL_NAME: ${MODEL_NAME}
  NUM_WORKERS_PER_QUEUE: ${NUM_WORKERS_PER_QUEUE:-8}
  NUQ_BACKEND: ${NUQ_BACKEND}
  OLLAMA_BASE_URL: ${OLLAMA_BASE_URL}
  OPENAI_API_KEY: ${OPENAI_API_KEY}
  OPENAI_BASE_URL: ${OPENAI_BASE_URL}
  PLAYWRIGHT_MICROSERVICE_URL: ${PLAYWRIGHT_MICROSERVICE_URL:-http://playwright-service:3000/scrape}
  POSTGRES_DB: ${POSTGRES_DB:-postgres}
  POSTGRES_HOST: ${POSTGRES_HOST:-nuq-postgres}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
  POSTGRES_PORT: ${POSTGRES_PORT:-5432}
  POSTGRES_USER: ${POSTGRES_USER:-postgres}
  PROXY_PASSWORD: ${PROXY_PASSWORD}
  PROXY_SERVER: ${PROXY_SERVER}
  PROXY_USERNAME: ${PROXY_USERNAME}
  REDIS_RATE_LIMIT_URL: ${REDIS_RATE_LIMIT_URL:-${REDIS_URL:-redis://redis:6379}}
  REDIS_URL: ${REDIS_URL:-redis://redis:6379}
  SEARXNG_CATEGORIES: ${SEARXNG_CATEGORIES}
  SEARXNG_ENDPOINT: ${SEARXNG_ENDPOINT}
  SEARXNG_ENGINES: ${SEARXNG_ENGINES}
  SELF_HOSTED_WEBHOOK_URL: ${SELF_HOSTED_WEBHOOK_URL}
  SLACK_WEBHOOK_URL: ${SLACK_WEBHOOK_URL}
  SUPABASE_ANON_TOKEN: ${SUPABASE_ANON_TOKEN}
  SUPABASE_SERVICE_TOKEN: ${SUPABASE_SERVICE_TOKEN}
  SUPABASE_URL: ${SUPABASE_URL}
  TEST_API_KEY: ${TEST_API_KEY}
  USE_DB_AUTHENTICATION: ${USE_DB_AUTHENTICATION:-false}
x-common-service:
  build: apps/api
  extra_hosts:
    - host.docker.internal:host-gateway
  logging:
    driver: json-file
    options:
      compress: "true"
      max-file: "3"
      max-size: 10m
  networks:
    - backend
  ulimits:
    nofile:
      hard: 65535
      soft: 65535
